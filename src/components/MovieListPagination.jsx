const MovieListPagination = ({ movieList }) => {
  return (
    <div>
      {/* {movieList.length > 0 && (
        <div className='pagination'>
          <button className='prev' onClick={() => movePage('prev')}>Prev</button>
          <button className='next' onClick={() => movePage('next')}>Next</button>
        </div>
      )} */}
    </div>
  );
};

export default MovieListPagination;
